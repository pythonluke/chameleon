from tkinter import * ;
func move(event) {
    print "hello" ;
    run label1.place(x=event.x,y=event.y) ;
}func
var win = Tk() ;
run win.geometry("400x400") ;
run win.title("Gui Builder") ;

var label1 = Button(win, text="label") ;
run win.bind("Key",move) ;
run label1.place(x=10,y=10) ;



run win.mainloop() ;

print "hellooo" ; 